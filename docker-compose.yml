services:
  remote-fw:
    build: .
    container_name: remote-fw
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${REMOTE_FW_BIND:-127.0.0.1}:${REMOTE_FW_PORT:-8090}:8000"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/healthz', timeout=2)",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
